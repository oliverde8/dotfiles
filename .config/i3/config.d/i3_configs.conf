# the vars file needs to be first so that other config files can use the vars
# defined within it.
import vars.conf;

import screen.conf;

import base.conf;
import colors.conf;
import menus.conf;
import my_custom.conf;
import workspaces.conf;
import startup.conf;
